<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingBottom="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="project.basit.friendschat.ChatsActivity"
    android:background="@drawable/whiteimg2"
    >

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_send"
        android:tint="@android:color/white"
        app:backgroundTint="@color/com_facebook_button_send_background_color"
        app:fabSize="mini" />


        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/input"
            android:hint="  Type Message..."
            android:layout_below="@+id/list_of_message"
            android:layout_toLeftOf="@+id/fab"
            android:layout_marginRight="8dp"
            android:background="@drawable/group_rect"
            android:backgroundTint="@color/tw__composer_white"
            android:layout_alignParentBottom="true"
            />


    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/list_of_message"
        android:layout_above="@+id/fab"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:divider="@android:color/transparent"
        android:dividerHeight="6dp"
        android:layout_marginBottom="16dp"
        android:transcriptMode="alwaysScroll"
        android:scrollbars="none"
        android:stackFromBottom="true">
    </ListView>
</RelativeLayout>
